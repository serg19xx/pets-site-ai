# Base URL for REST calls from the browser (no trailing slash).
# Leave empty to use same-origin paths and the Vite dev proxy for `/api` (see vite.config.ts).
# VITE_API_BASE_URL=http://localhost:8080
