# Copy to .env.production on the OVH host (do not commit secrets).
POSTGRES_USER=pets
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=pets

JWT_SECRET=change-me-long-random-secret
JWT_EXPIRES_IN=7d

FRONTEND_URL=https://pb.websmith-shop.com
AUTH_SIGNUP_DELIVERY=password

# Email (Resend or SMTP) — required for stranger signup
RESEND_API_KEY=
EMAIL_FROM=PETS Beta <onboarding@resend.dev>
